services:
  - type: web
    name: medicare-fraud-app
    env: docker
    plan: free # Note: The free plan might not have enough RAM for TensorFlow/PyTorch
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
      # as the app currently connects to localhost:3306
